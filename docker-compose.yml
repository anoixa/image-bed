services:
  image-bed:
    build: .
    container_name: image-bed
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
    volumes:
      - image-bed:/app/data
    # 从 .env 文件加载环境变量；不存在时使用镜像内默认配置
    env_file:
      - path: .env
        required: false
    environment:
      - SERVER_HOST=0.0.0.0
      - PUID=${PUID:-}
      - PGID=${PGID:-}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:${SERVER_PORT:-8080}/system/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  # PostgreSQL
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: image-bed-postgres
  #   environment:
  #     POSTGRES_USER: imagebed
  #     POSTGRES_PASSWORD: changeme
  #     POSTGRES_DB: image-bed
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped

  # Redis
  # redis:
  #   image: redis:7-alpine
  #   container_name: image-bed-redis
  #   restart: unless-stopped

volumes:
  image-bed:
